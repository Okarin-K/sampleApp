import { Button } from '@chakra-ui/react';

const CreateButton = () => {
    return (
        <Button type="submit" colorScheme="teal">
            投稿
        </Button>
    );
};

export default CreateButton;
